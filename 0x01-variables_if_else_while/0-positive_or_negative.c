#include <stdlib.h>
#include <time.h>
/**
 * main - prin positive and negative numbers
 * Return: 0 (Succes)
 */
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	return (0);
}
{
	if (n > o)
	{
	printf("%d is positive\n", a);
	}
	return (0);
}
